services:
  webhook-api:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data/events:/app/data/events
      - ./content/research/drafts:/app/content/research/drafts
    restart: unless-stopped
